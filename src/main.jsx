import React from "react"
import ReactDOM from "react-dom/client"
import ClusterVisApp from "./App.jsx"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ClusterVisApp />
  </React.StrictMode>
)
